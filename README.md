# Quickly Video

一个极简 Chrome 扩展：弹窗里有一个开关和一个倍速滑块。开启后，扩展会在网页中持续查找 `video` 元素，并按滑块选择的倍速播放；关闭后停止加速并把页面视频恢复为 1 倍速。

## 安装

1. 打开 Chrome，进入 `chrome://extensions/`。
2. 打开右上角「开发者模式」。
3. 点击「加载已解压的扩展程序」。
4. 选择本项目目录。

## 倍速

滑块支持 `2, 4, 6, 8, 10, 12, 14, 16` 倍速。
# quickly-video
